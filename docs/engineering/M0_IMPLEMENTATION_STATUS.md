# M0 Implementation Status

## Completed in this iteration
- Added pull-request quality gates in `.github/workflows/quality-gates.yml`:
  - Backend unit tests (`tests/test_phase1_unit.py`, `tests/test_migrations_manifest.py`)
  - **Mongo migrations (M0-4):** job `backend-mongo-migrations` — MongoDB 7 service, `mongo_migrate.py up` + `status` + second `up` (idempotent)
  - **RBAC static audit:** `python scripts/rbac_audit.py --strict`
  - Frontend production build (`yarn build`)
- **External secret stores (M0-2):** `backend/secrets_loader.py` + `SECRET_STORE`:
  - `vault` — HashiCorp Vault KV v2 (`hvac`), token or AppRole
  - `aws` — AWS Secrets Manager JSON secret (`boto3`, `AWS_SECRET_ID`)
  - Documented: `docs/runbooks/secret-stores.md`, `backend/.env.example`
- **RBAC audit script:** `backend/scripts/rbac_audit.py` + allowlist `rbac_audit_allowlist.json`
- Added secret scanning workflow in `.github/workflows/secret-scan.yml` using Gitleaks.
- Added dependency vulnerability scanning workflow in `.github/workflows/dependency-security.yml`:
  - Backend Python dependency scan via `pip-audit`
  - Frontend dependency scan via `yarn audit --level high`
- **Dependabot (weekly):** `.github/dependabot.yml` for `pip` (`/backend`), `npm` (`/frontend`), and `github-actions` (`/`)
- Added backend observability baseline:
  - **M0-3 structured logging:** `LOG_FORMAT=json` with **`service`**, **`request_id`**, `ts`, `level`, `logger`, `msg`; inbound **`X-Request-ID`** (or **`REQUEST_ID_HEADER`**); standard doc `docs/runbooks/structured-logging-standard.md`
  - **M0-3 Prometheus:** `GET /metrics` (`prometheus_client`) — `aai_http_requests_total{method,status_class}`, `aai_http_request_duration_seconds` histogram; optional **`PROMETHEUS_SCRAPE_TOKEN`**
  - Request/latency/error in-memory + **admin JSON** `GET /api/metrics` (JWT admin)
- **Grafana dashboard (as code):** `deploy/observability/grafana/dashboards/aai-hrms-api.json`
- **Prometheus alert rules (as code):** `deploy/observability/rules/aai-hrms-alerts.yml` (down, 5xx rate/ratio, p95 latency)
- **Local observability stack:** `deploy/observability/docker-compose.yml` + `deploy/observability/README.md`
- Added M0 operational runbooks:
  - `docs/runbooks/backup-restore.md`
  - `docs/runbooks/incident-response.md`
  - `docs/runbooks/migrations-and-rollback.md` (index/data change policy + rollback)
  - `docs/runbooks/logging-and-alerting.md` (log shipping, Prometheus/Grafana, alerts)
  - `docs/runbooks/structured-logging-standard.md` (JSON log field contract)
  - `docs/runbooks/dr-drill-log-template.md` (DR drill evidence template)
- **Mongo backup/restore automation scripts:**
  - `backend/scripts/mongo_backup.sh` — `mongodump` + tarball + `.meta.txt` (sha256)
  - `backend/scripts/mongo_restore.sh` — restore from tarball (`--drop`; use with approval)
- **Scheduled backup (GitHub Actions):** `.github/workflows/mongo-backup-scheduled.yml`
  - Weekly cron + `workflow_dispatch`
  - Requires secrets `MONGO_BACKUP_URL`, `MONGO_BACKUP_DB_NAME`
  - Uploads `backups/mongo/` as an artifact (14-day retention in workflow)
  - **Optional S3 — OIDC (preferred):** job `upload-s3-oidc` when `S3_BACKUP_BUCKET` + `AWS_ROLE_TO_ASSUME` are set (`id-token: write`)
  - **Optional S3 — static keys:** job `upload-s3-static` when bucket + access keys are set **and** `AWS_ROLE_TO_ASSUME` is **not** set
  - Optional for both: `AWS_REGION`, `S3_BACKUP_PREFIX`
- **Synthetic uptime probe:** `.github/workflows/uptime-probe.yml` — optional; enable with secret `UPTIME_PROBE_URL` (optional `UPTIME_PROBE_BEARER_TOKEN`); fails on non-200
- **Kubernetes sample:** `deploy/k8s/mongo-backup-cronjob.yaml` (CronJob + install database tools; add PVC/S3 per `deploy/README.md`)
- **Log shipping samples:** `docs/runbooks/samples/` (Fluent Bit + parser; `README.md`)
- **DR drill example (non-production):** `docs/runbooks/samples/dr-drill-example-stub.md` (illustrative only)
- **DR drill archive location:** `docs/compliance/dr-drills/README.md` (where to store real drill logs)
- **Deploy index:** `deploy/README.md` (secrets, backup workflow, S3 OIDC vs static, uptime probe, K8s notes)
- Added SBOM generation workflow in `.github/workflows/sbom.yml`:
  - Backend: `cyclonedx-py requirements` → `sbom-artifacts/sbom-backend.json`
  - Frontend: `@cyclonedx/cyclonedx-npm` → `sbom-artifacts/sbom-frontend.json`
  - Artifacts uploaded as **cyclonedx-sbom** on each workflow run
- Repo hygiene: `.gitignore` includes `sbom-artifacts/` and `backups/` (local Mongo dumps).
- **M0-4 Data migration / versioning:**
  - **Runner:** `backend/mongo_migrations_runner.py`, CLI `backend/scripts/mongo_migrate.py` (`up`, `up --dry-run`, `status`); state in **`_schema_migrations`**
  - **Migrations:** `backend/migrations/0001_schema_migrations_registry.py`, `0002_example_noop.py`, template `TEMPLATE_0000_description.py.example`, `backend/migrations/README.md`
  - **QA seed:** `backend/scripts/seed_qa_baseline.py` (idempotent admin, employee, skill, job; `_qa_seed` version marker)
  - **Restore validation:** `backend/scripts/validate_restore.py` — HTTP checks + optional `VALIDATE_MIN_USERS` Mongo count; wired into **`backend/scripts/run_phase1_tests_ci.sh`** after integration tests
  - **Runbooks:** `docs/runbooks/qa-seed-and-validate.md`; `migrations-and-rollback.md` updated for the runner
  - **Phase-1 CI script** `backend/scripts/run_phase1_tests_ci.sh` runs the same migration manifest unit test as quality gates
  - **`backend/pytest.ini`** — `pythonpath = .` so tests can import `mongo_migrations_runner` / `server` modules consistently (local + CI)

## Remaining for M0
- **Enable** workflows in GitHub: set `MONGO_BACKUP_URL` / `MONGO_BACKUP_DB_NAME`; optionally **`AWS_ROLE_TO_ASSUME`** + `S3_BACKUP_BUCKET` (OIDC) or static AWS keys; optionally `UPTIME_PROBE_URL` — see `deploy/README.md`.
- **Turn on Dependabot** in the GitHub repo settings if version updates are disabled at org level.
- **Apply** K8s CronJob (or equivalent) in your cluster with durable storage or object-storage upload.
- **Deploy** Fluent Bit (or platform agent) using `docs/runbooks/samples/` for log shipping; **wire Alertmanager** (or cloud alerting) to Prometheus rules in production.
- **Execute** a real DR drill, record it with `dr-drill-log-template.md`, and save a redacted copy under `docs/compliance/dr-drills/` (or your official compliance store).
