# Example secret — replace token before applying in production.
# kubectl create secret generic hiring-snapshot-token --from-literal=HIRING_SNAPSHOT_TOKEN='your-long-secret'
apiVersion: v1
kind: Secret
metadata:
  name: hiring-snapshot-token
  labels:
    app: aai-hrms
type: Opaque
stringData:
  HIRING_SNAPSHOT_TOKEN: "change-me-in-production"
