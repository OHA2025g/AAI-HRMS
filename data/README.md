# Local sample data

Place Excel workbooks used by Docker auto-import here:

- `excel/Candidates 1.xlsx`
- `excel/Job Descriptions 1.xlsx`

These map into the API container at `/data/excel/…` via `docker-compose.yml`.
Workbooks are gitignored; keep copies locally for seed/import.
